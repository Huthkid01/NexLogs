-- OPTIONAL — only if you deploy server/ to Render or another host.
-- Skip this file if you use the Supabase Edge Function only (hostinger_transactional_emails_ready.sql).

-- INSERT INTO app_config (key, value) VALUES
--   ('email_api_base', 'https://your-email-api.example.com')
-- ON CONFLICT (key) DO UPDATE SET value = EXCLUDED.value, updated_at = NOW();
